local function run(msg)
if msg.text == "avatar" then
	return "avatar world new  anti spam  developer  @Ali_dev_avatar_TG"
end
end

return {
	description = "AVATAR", 
	usage = "AVATART TG",
	patterns = {
		"^avatar",
		}, 
	run = run,
    --privileged = true,
	pre_process = pre_process
}
